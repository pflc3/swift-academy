import SwiftUI

// Main container view that organizes the chat interface layout
struct ChatContentView: View {
    var body: some View {
        VStack {
            Text("Hey")
        }
    }
}

#Preview {
    ChatContentView()
}
